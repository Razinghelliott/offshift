{
  "name": "offshift-functions",
  "description": "OFFSHIFT Cloud Functions — signup notifications",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo 'Skipping lint (no eslint configured)'",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "resend": "^4.0.1"
  },
  "private": true
}
